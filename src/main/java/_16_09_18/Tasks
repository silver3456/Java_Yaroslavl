
1. Дан справочник со столбцами Фамилия, Зарплата(проставить 5-6 строк на свое усмотрение с различными значениями, 2 из которых - совпадают по фамилии и зарплате)
Необходимо создать объектную модель данного справочника, переопределить методы equals(obj) и hashCode() для сравнивания по полям. Записать объекты в HashSet, вывести сет на экран.
Затем написать функцию удаляющую объект из Set'a, у которого поле "Зарплата" ниже определенного значения(параметр) при помощи методов Set'а.
Вывести Set на экран

2. Справочник тот же и объекты те же. Работа с TreeSet. При создании объекта TreeSet параметром конструктора можно указать компаратор: TreeSet(Comparator<? super E> comparator).
Необходимо добавить объекты с использованием компаратора, вывести объекты на экран. Создать новый Set с другим компаратором, добавить те же объекты, вывести на экран. Для промежуточного хранения объектов можно использовать литеральный массив = {new Obj(), new Obj(),...};

3. Разобраться в работе HashSet и TreeSet, найти отличия в работе и рассказать на уроке.

4. Те же самые объекты. Необходимо написать метод, фильтрующий множество по объектам, Фамилия в которых содержит определенный символ(параметр). Использовать Stream API. и метод removeIf. Понять главное отличие этих 2х подходов.